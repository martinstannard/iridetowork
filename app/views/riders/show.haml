.rider
  = @from.ll
  to
  = @to.ll
.description
  = @rider.description
.rider_image
  - if @rider.image
    = image_tag @rider.image.public_filename(:small) 

.div{:onclick => "initMap(#{@from.lat} , #{@from.lng}, #{@to.lat} , #{@to.lng});"}
  Show Map


.map#map_canvas{:width => 500, :height => 600}
= link_to 'Edit', :action => 'edit', :id => @rider
\|
= link_to 'Back', :action => 'index'
