.rider
  = @from.postal_code
  to
  = @to.postal_code

.div{:onclick => "initMap(#{@from.lat} , #{@from.long}, #{@to.lat} , #{@to.long});"}
  Show Map


.map#map_canvas{:width => 500, :height => 600}
= link_to 'Edit', :action => 'edit', :id => @rider
\|
= link_to 'Back', :action => 'index'
